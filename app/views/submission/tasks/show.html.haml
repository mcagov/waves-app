- content_for :task_body do
  = render partial: "submissions/#{Activity.new(@submission.part).view_mode}/show",
    locals: { submission: @submission }

= render partial: "base", locals: { task: @task, submission: @submission }
