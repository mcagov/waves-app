.x_panel
  %h2.x_title Tasks for this Application
  .x_content
    %table.table
      %thead
        %th Task
        %th Service Level
        %th Price
        %th Start Date
        %th Target Date
        %th Reference No
        %th Status
      %tbody
        - @submission.tasks.includes(:service).each do |task|
          %tr
            %td.service_name= task.service.name
            %td.service_level= task.service_level.to_s.humanize
            %td.formatted_price= formatted_amount(task.price)
            %th.start_date= task.start_date
            %th.target_date= task.target_date
            %th.ref_no= task.ref_no
            %th.state= task.current_state.to_s.humanize
