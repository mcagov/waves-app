- if @submission.claimant == current_user
  = content_for :primary_action_button do
    = link_to "Convert to Application",
      convert_submission_finance_payment_path(@submission),
      method: :patch,
      class: "btn btn-lg btn-primary pull-right"
