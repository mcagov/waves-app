:plain
  $("##{ @modal_id }").modal('hide');
  $('.modal-backdrop').remove();
  $('body').removeClass('modal-open');


  $('#fees').html(
    '#{escape_javascript(
      render partial: "/submissions/info/fees",
      locals: { submission: @submission })}'
  );
