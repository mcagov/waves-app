:plain
  if ("#{ @modal_id }".length > 0) {
    $("##{ @modal_id }").modal('hide');
    $('.modal-backdrop').remove();
    $('body').removeClass('modal-open');
  }

  $("#directed_by").html(
    '#{escape_javascript(
       render partial: "/submissions/extended/forms/directed_by/base")}'
  );
